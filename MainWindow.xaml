<Window x:Class="MISMC.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:c1="clr-namespace:MyMVVM"
        mc:Ignorable="d"
        Title="MainWindow" Height="160" Width="231">
    <Grid>
        <StackPanel>
            <Label>用户名</Label>
            <TextBox Text="{Binding UserName}"></TextBox>
            <Label>密码</Label>
            <TextBox Text="{Binding UserPass}"></TextBox>
            <Grid >
                <Button Content="登陆">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <c1:MyEventCommand Command="{Binding  BtLogin}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers> 
                </Button>
            </Grid>
            <Grid >
                <Button>注册</Button>
            </Grid>
        </StackPanel>
        
    </Grid>
</Window>
